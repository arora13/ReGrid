VITE_MAPBOX_TOKEN=
# Server-only (never prefix with VITE_): copilot intent + short plain-English summary via Anthropic.
ANTHROPIC_API_KEY=
# Optional override (default: claude-3-5-haiku-20241022 — keep this for lowest cost per copilot run)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# Optional: max output tokens per intent call (256–1024, default 512). Lower = cheaper.
# ANTHROPIC_MAX_OUTPUT_TOKENS=512
VITE_CONVEX_URL=
VITE_SPATIAL_API_URL=http://localhost:8000
